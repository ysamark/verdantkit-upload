import { describe, expect, it } from "vitest";

describe("Test resolveFormDataObjectWithFileVariants UploadClient util", () => {
  it("should say ok to vitest", () => {
    expect(false).not.toBeTruthy();
  });
});
